{
    port = 8080
    timeout = 30 seconds
    github: {
        token = "dummy"
        token = ${?GH_TOKEN}
        max-retry-count = 2
        base-path = "https://api.github.com"
        page-size = 30
        cache-expiration = 60 seconds
    }
}